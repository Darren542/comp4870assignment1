@model ClassLibrary.Models.Member

@{
    ViewData["Title"] = "Create Member";
}

<component type="typeof(CreateMember)" render-mode="ServerPrerendered" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
