@model ClassLibrary.Models.Manifest

@{
    ViewData["Title"] = "Create Manifest";
}

<component type="typeof(CreateManifest)" render-mode="ServerPrerendered" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
