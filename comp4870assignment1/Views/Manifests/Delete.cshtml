@model ClassLibrary.Models.Manifest

@{
    ViewData["Title"] = "Delete Manifest";
}

<component type="typeof(DeleteManifest)" render-mode="ServerPrerendered" param-ManifestId="@Model.ManifestId" param-MemberId="@Model.MemberId" />
